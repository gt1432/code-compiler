services:
  - type: web
    name: code-compiler
    runtime: docker
    plan: free
    envVars:
      - key: SPRING_DATA_MONGODB_URI
        sync: false # You will set this in the dashboard or I'll use a placeholder
      - key: PORT
        value: 8080
